package gov.pnnl.svf.vbo;

/**
 * Interface used to create required vertex buffer objects (VBO)s.
 *
 * @author Arthur Bleeker
 */
public interface VboDrawableItem extends PreparableItem, VboDrawable, VboPickable, VboColorPickable {
}
